<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.myapp.manju.arstrike.MainActivity">

    <org.opencv.android.JavaCameraView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/java_camera_view"
        />

    <Button
        android:text="Render"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="41dp"
        android:id="@+id/render_button"
        tools:text="Render"
        android:onClick="onRenderClick" />

    <Button
        android:text="Engine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="15dp"
        android:id="@+id/engine_button"
        tools:text="Engine"
        android:onClick="onEngineClick (MainActivity)"
        android:layout_above="@+id/render_button"
        android:layout_alignLeft="@+id/render_button"
        android:layout_alignStart="@+id/render_button" />
        android:layout_marginBottom="63dp"
        android:id="@+id/BRender"
        android:onClick="onButtonClick" />

</RelativeLayout>
